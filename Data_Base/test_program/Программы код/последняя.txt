unit menu;

interface

procedure v_menu;

implementation

uses crt;

procedure about_for_me;
begin
  textbackground(red);
  textcolor(white);
  writeln('/*');
  writeln('Подключен модуль menu');
  writeln('@Harlamov Anton P-202');
  writeln('Begin : 10.02.2014');
  writeln('Version : 0.1 ');
  writeln('*/');
  textbackground(black);
end;

procedure v_menu;
const
  end_m = 4;
var
  msfm: array[1..11] of string;
  i:integer;
  f:char;
//Процедура,предназначеная для заполнение массива текстом
//Каждый элемент новая строка меню
{procedure text_for_menu(var a:array of string);
begin
end;}


  //Процедура,предназначеная для вывода end_m мерного массива
  // Входимые данные (массив строк A)
 { procedure vivod_text(var a: array of string);
  var i:integer;
  begin

  end;}
  //Функция,предназначена для перемещения цветного выделения
  //vxod массив строк
  function kursor(var vxod: array of string):integer;
  var x,y,z:integer;
      c:char;
  begin
    y:=1;gotoxy(1,y);
    c:=readkey;
    if c=#0 then
    begin
      c:=readkey;
      case Ord(c) of
        72:begin
             if y<>1 then
             begin
               textbackground(black);write(' ',vxod[y], ' ');
               y:=y-1;gotoxy(1,y);
               textbackground(red);write(#16,vxod[y],#17);
             end;
           end;
        80:begin
             if y<>end_m then
             begin
               textbackground(black);write(' ', vxod[y], ' ');
               y:=y+1;gotoxy(1,y);
               textbackground(red);write(#16,vxod[y],#17);
             end;
           end;
      end;
    end;
    kursor:=y;
  end;

begin
  gotoxy(1,1);
  msfm[1]:='Первая таблица';
  msfm[2]:='Вторая таблица';
  msfm[3]:='Разработчик';
  msfm[4]:='Выход';
  for i:=1 to end_m do
  begin
    if i=1 then
    begin
      textbackground(red);
      writeln(#16,msfm[i], #17);
      textbackground(black);
    end
    else writeln(' ', msfm[i], ' ');
  end;
  repeat
    kursor(msfm);
    f:=readkey;
  until f=#13;
  //about_for_me;
end;

end.